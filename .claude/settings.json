{
  "hooks": {
    "postToolUse": [
      {
        "matcher": "Edit|Write",
        "command": "bash -c 'CHANGED=$(git diff --name-only HEAD | grep -E \"\\.rs$\" | head -1); if [ -n \"$CHANGED\" ]; then cargo check --workspace 2>&1 | tail -5; fi'"
      }
    ]
  }
}
